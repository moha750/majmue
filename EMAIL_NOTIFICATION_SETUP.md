# دليل إعداد نظام الإشعارات عبر البريد الإلكتروني

## 📋 نظرة عامة

تم تنفيذ نظام إشعارات البريد الإلكتروني لإدارة طلبات نشر الاستبيانات باستخدام **EmailJS**. يرسل النظام إشعارات تلقائية عند:
- ✅ **قبول طلب النشر**: رسالة ثابتة مع معلومات الاستبيان ورابط الوصول
- ❌ **رفض طلب النشر**: رسالة مخصصة يكتبها المسؤول توضح سبب الرفض

---

## 🏗️ معمارية النظام (Architecture)

### تدفق العملية (Workflow)

#### 1. عملية القبول (Approval Flow)
```
المسؤول يضغط "قبول" 
    ↓
تأكيد الإجراء
    ↓
إنشاء استبيان في قاعدة البيانات
    ↓
توليد رابط داخلي فريد (slug)
    ↓
تحديث حالة الطلب إلى "approved"
    ↓
إرسال بريد إلكتروني تلقائي (قالب ثابت)
    ↓
عرض رسالة نجاح للمسؤول
    ↓
تحديث واجهة المستخدم
```

#### 2. عملية الرفض (Rejection Flow)
```
المسؤول يضغط "رفض"
    ↓
فتح نافذة منبثقة
    ↓
المسؤول يكتب سبب الرفض (رسالة مخصصة)
    ↓
التحقق من صحة الإدخال (20 حرف على الأقل)
    ↓
تحديث حالة الطلب إلى "rejected"
    ↓
إرسال بريد إلكتروني مع السبب المخصص
    ↓
عرض رسالة نجاح للمسؤول
    ↓
تحديث واجهة المستخدم
```

---

## 📦 الملفات المضافة/المعدلة

### ملفات جديدة:
1. **`js/emailService.js`** - خدمة البريد الإلكتروني الرئيسية
2. **`EMAIL_TEMPLATES.md`** - قوالب HTML للبريد الإلكتروني
3. **`EMAIL_NOTIFICATION_SETUP.md`** - هذا الملف (دليل الإعداد)

### ملفات معدلة:
1. **`admin.html`** - إضافة مكتبة EmailJS والنافذة المنبثقة
2. **`css/admin.css`** - تنسيقات النافذة المنبثقة
3. **`js/admin.js`** - تحديث دوال القبول والرفض

---

## ⚙️ خطوات الإعداد (Setup Instructions)

### الخطوة 1: إنشاء حساب EmailJS

1. انتقل إلى [EmailJS](https://www.emailjs.com/)
2. اضغط على **Sign Up** وأنشئ حساب مجاني
3. قم بتأكيد بريدك الإلكتروني

### الخطوة 2: إضافة خدمة البريد الإلكتروني

1. من لوحة التحكم، اذهب إلى **Email Services**
2. اضغط على **Add New Service**
3. اختر مزود البريد (Gmail موصى به):
   - **Gmail**: سهل الإعداد ومجاني
   - **Outlook**: بديل جيد
   - **Custom SMTP**: للخوادم المخصصة
4. اتبع التعليمات لربط حسابك
5. **احفظ Service ID** (مثال: `service_abc123`)

### الخطوة 3: الحصول على Public Key

1. اذهب إلى **Account** > **General**
2. في قسم **API Keys**، انسخ **Public Key**
3. احفظه (مثال: `abcdefg123456789`)

### الخطوة 4: إنشاء قوالب البريد الإلكتروني

#### قالب القبول (Approval Template)

1. اذهب إلى **Email Templates**
2. اضغط على **Create New Template**
3. املأ المعلومات:
   - **Template Name**: `Survey Approval Notification`
   - **Template ID**: `template_approval`
4. في قسم **Settings**:
   - **To Email**: `{{to_email}}`
   - **From Name**: `منصة مجموع`
   - **Subject**: `تمت الموافقة على طلب نشر استبيانك - منصة مجموع`
5. في قسم **Content**:
   - انسخ محتوى HTML من ملف `EMAIL_TEMPLATES.md` (قالب القبول)
6. في قسم **Plain Text**:
   - انسخ المحتوى النصي البديل
7. اضغط **Save**

#### قالب الرفض (Rejection Template)

1. اضغط على **Create New Template** مرة أخرى
2. املأ المعلومات:
   - **Template Name**: `Survey Rejection Notification`
   - **Template ID**: `template_rejection`
3. في قسم **Settings**:
   - **To Email**: `{{to_email}}`
   - **From Name**: `منصة مجموع`
   - **Subject**: `تحديث بخصوص طلب نشر استبيانك - منصة مجموع`
4. في قسم **Content**:
   - انسخ محتوى HTML من ملف `EMAIL_TEMPLATES.md` (قالب الرفض)
5. في قسم **Plain Text**:
   - انسخ المحتوى النصي البديل
6. اضغط **Save**

### الخطوة 5: تحديث إعدادات المشروع

افتح ملف `js/emailService.js` وحدث الإعدادات:

```javascript
const EMAIL_CONFIG = {
    serviceId: 'service_abc123',        // ضع Service ID هنا
    publicKey: 'abcdefg123456789',      // ضع Public Key هنا
    templates: {
        approval: 'template_approval',   // معرف قالب القبول
        rejection: 'template_rejection'  // معرف قالب الرفض
    }
};
```

### الخطوة 6: اختبار النظام

1. افتح `admin.html` في المتصفح
2. سجل الدخول كمسؤول
3. جرب قبول طلب:
   - اضغط على زر "قبول"
   - تحقق من وصول البريد الإلكتروني
4. جرب رفض طلب:
   - اضغط على زر "رفض"
   - اكتب سبب الرفض
   - تحقق من وصول البريد مع السبب المخصص

---

## 🔧 التفاصيل التقنية

### 1. خدمة البريد الإلكتروني (`emailService.js`)

#### الوظائف الرئيسية:

**`initEmailJS()`**
- تهيئة مكتبة EmailJS بالمفتاح العام
- يتم استدعاؤها تلقائياً عند تحميل الصفحة

**`sendApprovalEmail(requestData)`**
- **المدخلات**:
  ```javascript
  {
    requester_email: string,
    requester_name: string,
    survey_title: string,
    survey_description: string,
    internal_link: string
  }
  ```
- **المخرجات**: `{ success: boolean, message: string, error?: any }`
- **الوظيفة**: إرسال إشعار القبول بقالب ثابت

**`sendRejectionEmail(requestData, rejectionReason)`**
- **المدخلات**:
  ```javascript
  requestData: {
    requester_email: string,
    requester_name: string,
    survey_title: string
  },
  rejectionReason: string  // الرسالة المخصصة من المسؤول
  ```
- **المخرجات**: `{ success: boolean, message: string, error?: any }`
- **الوظيفة**: إرسال إشعار الرفض مع السبب المخصص

**`validateEmail(email)`**
- التحقق من صحة صيغة البريد الإلكتروني

### 2. تحديثات admin.js

#### دالة القبول المحدثة:
```javascript
async function approveRequest(requestId) {
    // 1. جلب بيانات الطلب
    // 2. إنشاء استبيان جديد
    // 3. توليد slug فريد
    // 4. تحديث حالة الطلب
    // 5. إرسال إشعار البريد الإلكتروني
    // 6. عرض رسالة نجاح
    // 7. تحديث الواجهة
}
```

#### دالة الرفض المحدثة:
```javascript
async function rejectRequest(requestId) {
    // 1. جلب بيانات الطلب
    // 2. عرض النافذة المنبثقة
    // 3. انتظار إدخال المسؤول
}

async function confirmRejection() {
    // 1. التحقق من صحة الإدخال (20 حرف على الأقل)
    // 2. تحديث حالة الطلب
    // 3. إرسال إشعار البريد مع السبب
    // 4. إغلاق النافذة
    // 5. عرض رسالة نجاح
    // 6. تحديث الواجهة
}
```

### 3. النافذة المنبثقة (Rejection Modal)

**الميزات**:
- تصميم عصري ومتجاوب
- إغلاق عند النقر خارج النافذة
- إغلاق عند الضغط على ESC
- التحقق من صحة الإدخال
- رسائل خطأ واضحة
- أمثلة توضيحية في placeholder

**التحقق من الصحة**:
- الحد الأدنى: 20 حرف
- لا يمكن أن يكون فارغاً
- يجب أن يكون نصاً واضحاً ومفيداً

---

## 📊 المتغيرات المستخدمة في القوالب

### قالب القبول:
| المتغير | الوصف | مثال |
|---------|-------|------|
| `{{to_email}}` | بريد المستلم | user@example.com |
| `{{to_name}}` | اسم المستلم | أحمد محمد |
| `{{survey_title}}` | عنوان الاستبيان | استبيان رضا العملاء |
| `{{survey_description}}` | وصف الاستبيان | استبيان لقياس... |
| `{{internal_link}}` | رابط الاستبيان | https://site.com/survey.html?s=abc123 |
| `{{platform_name}}` | اسم المنصة | منصة مجموع |
| `{{current_year}}` | السنة الحالية | 2026 |

### قالب الرفض:
| المتغير | الوصف | مثال |
|---------|-------|------|
| `{{to_email}}` | بريد المستلم | user@example.com |
| `{{to_name}}` | اسم المستلم | أحمد محمد |
| `{{survey_title}}` | عنوان الاستبيان | استبيان رضا العملاء |
| `{{rejection_reason}}` | سبب الرفض (مخصص) | الاستبيان لا يتوافق مع... |
| `{{platform_name}}` | اسم المنصة | منصة مجموع |
| `{{current_year}}` | السنة الحالية | 2026 |

---

## 🎨 التخصيص

### تعديل تصميم البريد الإلكتروني

يمكنك تعديل القوالب في لوحة تحكم EmailJS:
1. افتح القالب المراد تعديله
2. عدّل HTML/CSS حسب الحاجة
3. استخدم **Test It** لمعاينة التغييرات
4. احفظ التعديلات

### تعديل ألوان المنصة

في قوالب HTML، عدّل الألوان:
```css
/* اللون الأساسي */
background: linear-gradient(135deg, #317157 0%, #245a44 100%);

/* لون الأزرار */
background-color: #317157;
```

### إضافة شعار المنصة

في قسم `<div class="header">`:
```html
<img src="https://yoursite.com/logo.png" alt="Logo" style="max-width: 150px;">
<h1>{{platform_name}}</h1>
```

---

## 🔒 الأمان والخصوصية

### أفضل الممارسات:

1. **لا تشارك المفاتيح**:
   - احتفظ بـ Service ID و Public Key سرياً
   - لا تضعها في مستودعات عامة (GitHub)

2. **استخدام متغيرات البيئة** (اختياري):
   ```javascript
   const EMAIL_CONFIG = {
       serviceId: process.env.EMAILJS_SERVICE_ID,
       publicKey: process.env.EMAILJS_PUBLIC_KEY,
       // ...
   };
   ```

3. **التحقق من البريد الإلكتروني**:
   - يتم التحقق من صحة البريد قبل الإرسال
   - استخدام regex للتحقق من الصيغة

4. **حماية من Spam**:
   - EmailJS يوفر حماية مدمجة
   - يمكن تفعيل CAPTCHA في الإعدادات

---

## 📈 الحدود والقيود

### الخطة المجانية (EmailJS):
- **200 رسالة/شهر**
- **حجم الرسالة**: 50 KB
- **المرفقات**: غير مدعومة في الخطة المجانية
- **معدل الإرسال**: 1 رسالة/ثانية

### الترقية:
إذا احتجت لأكثر من 200 رسالة/شهر:
1. اذهب إلى **Pricing** في EmailJS
2. اختر خطة مدفوعة (تبدأ من $15/شهر)
3. احصل على:
   - 1000+ رسالة/شهر
   - دعم المرفقات
   - معدل إرسال أعلى
   - دعم فني أولوية

---

## 🐛 استكشاف الأخطاء (Troubleshooting)

### المشكلة: البريد لا يصل

**الحلول**:
1. ✅ تحقق من صحة البريد الإلكتروني للمستلم
2. ✅ تحقق من مجلد Spam/Junk
3. ✅ تحقق من صحة Service ID و Public Key
4. ✅ تحقق من صحة Template IDs
5. ✅ راجع Console في المتصفح للأخطاء
6. ✅ تحقق من حدود الاستخدام في حسابك

### المشكلة: خطأ "EmailJS library not loaded"

**الحلول**:
1. تأكد من تحميل مكتبة EmailJS في `admin.html`:
   ```html
   <script src="https://cdn.jsdelivr.net/npm/@emailjs/browser@3/dist/email.min.js"></script>
   ```
2. تحقق من اتصال الإنترنت
3. جرب تحديث الصفحة

### المشكلة: خطأ "Invalid template"

**الحلول**:
1. تحقق من صحة Template IDs في `emailService.js`
2. تأكد من إنشاء القوالب في EmailJS
3. تأكد من تطابق الأسماء بالضبط

### المشكلة: البريد يصل بدون تنسيق

**الحلول**:
1. تأكد من نسخ HTML بالكامل في القالب
2. تحقق من دعم عميل البريد للـ HTML
3. تأكد من وجود محتوى Plain Text كبديل

### المشكلة: المتغيرات لا تظهر بشكل صحيح

**الحلول**:
1. تأكد من استخدام `{{variable_name}}` في القالب
2. تحقق من إرسال القيم الصحيحة من JavaScript
3. راجع أسماء المتغيرات (حساسة لحالة الأحرف)

---

## 🧪 اختبار النظام

### اختبار يدوي:

1. **اختبار القبول**:
   ```
   1. افتح admin.html
   2. اذهب إلى "طلبات الاستبيانات"
   3. اضغط "قبول" على طلب
   4. تحقق من:
      - ظهور رسالة نجاح
      - تحديث حالة الطلب
      - وصول البريد الإلكتروني
      - صحة الرابط في البريد
   ```

2. **اختبار الرفض**:
   ```
   1. افتح admin.html
   2. اذهب إلى "طلبات الاستبيانات"
   3. اضغط "رفض" على طلب
   4. تحقق من:
      - فتح النافذة المنبثقة
      - عدم قبول نص قصير (<20 حرف)
      - إرسال البريد مع السبب المخصص
      - إغلاق النافذة عند ESC
      - إغلاق النافذة عند النقر خارجها
   ```

### اختبار من EmailJS:

1. افتح القالب في EmailJS
2. اضغط **Test It**
3. أدخل قيم تجريبية:
   ```
   to_email: your-test@email.com
   to_name: اسم تجريبي
   survey_title: عنوان تجريبي
   rejection_reason: سبب تجريبي للرفض
   ```
4. اضغط **Send Test Email**
5. تحقق من وصول البريد

---

## 📝 ملاحظات مهمة

### 1. دعم اللغة العربية
- جميع القوالب تدعم UTF-8
- التصميم RTL (من اليمين لليسار)
- الخطوط تدعم العربية بشكل كامل

### 2. التوافق مع المتصفحات
- Chrome ✅
- Firefox ✅
- Safari ✅
- Edge ✅
- IE 11 ❌ (غير مدعوم)

### 3. الأداء
- الإرسال غير متزامن (async)
- لا يعطل واجهة المستخدم
- معالجة الأخطاء بشكل سليم

### 4. قابلية التوسع
- يمكن إضافة قوالب جديدة بسهولة
- يمكن إضافة إشعارات أخرى (تذكيرات، تحديثات، إلخ)
- البنية معيارية وقابلة للصيانة

---

## 🚀 التطوير المستقبلي

### ميزات مقترحة:

1. **إشعارات إضافية**:
   - إشعار عند تعديل الاستبيان
   - تذكير دوري للاستبيانات غير النشطة
   - إشعار عند وصول عدد معين من الضغطات

2. **تحسينات**:
   - معاينة البريد قبل الإرسال
   - قوالب متعددة للاختيار
   - إحصائيات معدل فتح البريد
   - سجل الإشعارات المرسلة

3. **تكامل**:
   - دعم مزودي بريد آخرين (SendGrid, Mailgun)
   - إشعارات SMS
   - إشعارات Push

---

## 📞 الدعم والمساعدة

### موارد مفيدة:
- [توثيق EmailJS](https://www.emailjs.com/docs/)
- [أمثلة EmailJS](https://www.emailjs.com/examples/)
- [منتدى EmailJS](https://www.emailjs.com/community/)

### في حالة المشاكل:
1. راجع قسم استكشاف الأخطاء أعلاه
2. تحقق من Console في المتصفح
3. راجع سجل الأخطاء في EmailJS Dashboard
4. تواصل مع دعم EmailJS

---

## ✅ قائمة التحقق النهائية

قبل النشر، تأكد من:

- [ ] إنشاء حساب EmailJS
- [ ] إضافة خدمة البريد الإلكتروني
- [ ] إنشاء قالب القبول
- [ ] إنشاء قالب الرفض
- [ ] تحديث `emailService.js` بالمفاتيح الصحيحة
- [ ] اختبار إرسال بريد القبول
- [ ] اختبار إرسال بريد الرفض
- [ ] التحقق من وصول البريد بشكل صحيح
- [ ] التحقق من التنسيق والتصميم
- [ ] اختبار على أجهزة مختلفة
- [ ] مراجعة الأمان والخصوصية

---

**تم التنفيذ بنجاح! 🎉**

النظام جاهز للاستخدام بعد إكمال خطوات الإعداد.
