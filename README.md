# منصة مجموع - منصة الاستبيانات والبحوث العلمية

منصة رقمية عربية متخصصة في نشر ومشاركة الاستبيانات والبحوث العلمية، مبنية باستخدام HTML5، CSS3، Vanilla JavaScript، وSupabase.

## 📋 المحتويات

- [نظرة عامة](#نظرة-عامة)
- [المميزات](#المميزات)
- [التقنيات المستخدمة](#التقنيات-المستخدمة)
- [هيكل المشروع](#هيكل-المشروع)
- [التثبيت والإعداد](#التثبيت-والإعداد)
- [إعداد قاعدة البيانات](#إعداد-قاعدة-البيانات)
- [الاستخدام](#الاستخدام)
- [الصفحات](#الصفحات)

## 🎯 نظرة عامة

منصة مجموع هي منصة ويب عربية تتيح للباحثين والأفراد نشر استبياناتهم بهدف جمع أكبر عدد ممكن من الردود. توفر المنصة واجهة سهلة الاستخدام لعرض الاستبيانات حسب التصنيفات المختلفة، مع لوحة تحكم إدارية كاملة.

## ✨ المميزات

### للزوار
- **عرض الاستبيانات**: تصفح جميع الاستبيانات المتاحة
- **التصنيفات**: فلترة الاستبيانات حسب التصنيف (عامة، طبية، قانونية، إلخ)
- **طلب نشر استبيان**: نموذج سهل لطلب نشر استبيان جديد
- **التواصل**: نموذج تواصل مباشر مع إدارة المنصة

### للمشرفين
- **لوحة تحكم شاملة**: إدارة كاملة للمنصة
- **إحصائيات**: عرض إحصائيات الزيارات والطلبات والرسائل
- **إدارة الطلبات**: قبول أو رفض طلبات نشر الاستبيانات
- **إدارة التصنيفات**: إضافة، تعديل، وحذف التصنيفات
- **إدارة الرسائل**: عرض وحذف رسائل التواصل
- **إدارة الاستبيانات**: عرض وحذف الاستبيانات المنشورة

## 🛠️ التقنيات المستخدمة

- **HTML5**: بنية الصفحات
- **CSS3**: التصميم والتنسيق (تصميم متجاوب، RTL)
- **Vanilla JavaScript**: البرمجة والتفاعل
- **Supabase**: قاعدة البيانات والمصادقة
  - Authentication (تسجيل الدخول)
  - Database (PostgreSQL)
  - Real-time subscriptions

## 📁 هيكل المشروع

```
majmue/
│
├── index.html              # الصفحة الرئيسية
├── request.html            # صفحة طلب نشر استبيان
├── admin.html              # لوحة التحكم
├── README.md               # ملف التوثيق
│
├── css/
│   ├── style.css          # الأنماط الرئيسية
│   └── admin.css          # أنماط لوحة التحكم
│
└── js/
    ├── config.js          # إعدادات Supabase
    ├── main.js            # سكريبت الصفحة الرئيسية
    ├── request.js         # سكريبت صفحة الطلبات
    └── admin.js           # سكريبت لوحة التحكم
```

## 🚀 التثبيت والإعداد

### 1. تحميل المشروع

قم بتحميل جميع ملفات المشروع إلى مجلد على جهازك.

### 2. إنشاء مشروع Supabase

1. انتقل إلى [Supabase](https://supabase.com)
2. قم بإنشاء حساب جديد أو تسجيل الدخول
3. أنشئ مشروعًا جديدًا
4. احفظ معلومات المشروع:
   - Project URL
   - Anon/Public Key

### 3. تكوين الاتصال بـ Supabase

افتح ملف `js/config.js` وقم بتحديث المعلومات التالية:

```javascript
const SUPABASE_URL = 'YOUR_SUPABASE_URL'; // ضع رابط مشروعك هنا
const SUPABASE_ANON_KEY = 'YOUR_SUPABASE_ANON_KEY'; // ضع المفتاح العام هنا
```

## 🗄️ إعداد قاعدة البيانات

### إنشاء الجداول

انتقل إلى **SQL Editor** في لوحة تحكم Supabase وقم بتشغيل الأوامر التالية:

```sql
-- جدول التصنيفات
CREATE TABLE categories (
    id BIGSERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- جدول الاستبيانات المنشورة
CREATE TABLE surveys (
    id BIGSERIAL PRIMARY KEY,
    title TEXT NOT NULL,
    description TEXT,
    category_id BIGINT REFERENCES categories(id),
    survey_link TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- جدول طلبات نشر الاستبيانات
CREATE TABLE survey_requests (
    id BIGSERIAL PRIMARY KEY,
    requester_name TEXT NOT NULL,
    requester_email TEXT NOT NULL,
    survey_title TEXT NOT NULL,
    survey_description TEXT,
    category_id BIGINT REFERENCES categories(id),
    survey_link TEXT NOT NULL,
    status TEXT DEFAULT 'pending',
    created_at TIMESTAMP DEFAULT NOW()
);

-- جدول رسائل التواصل
CREATE TABLE contact_messages (
    id BIGSERIAL PRIMARY KEY,
    name TEXT NOT NULL,
    email TEXT NOT NULL,
    message TEXT NOT NULL,
    created_at TIMESTAMP DEFAULT NOW()
);

-- جدول زيارات الموقع
CREATE TABLE site_visits (
    id BIGSERIAL PRIMARY KEY,
    visit_date DATE DEFAULT CURRENT_DATE,
    visit_count INTEGER DEFAULT 1,
    created_at TIMESTAMP DEFAULT NOW()
);

-- إدراج تصنيفات افتراضية
INSERT INTO categories (name) VALUES 
    ('استبيانات عامة'),
    ('استبيانات طبية'),
    ('استبيانات قانونية');
```

### إعداد Row Level Security (RLS)

```sql
-- تفعيل Row Level Security
ALTER TABLE categories ENABLE ROW LEVEL SECURITY;
ALTER TABLE surveys ENABLE ROW LEVEL SECURITY;
ALTER TABLE survey_requests ENABLE ROW LEVEL SECURITY;
ALTER TABLE contact_messages ENABLE ROW LEVEL SECURITY;
ALTER TABLE site_visits ENABLE ROW LEVEL SECURITY;

-- سياسات القراءة العامة
CREATE POLICY "Enable read access for all users" ON categories FOR SELECT USING (true);
CREATE POLICY "Enable read access for all users" ON surveys FOR SELECT USING (true);

-- سياسات الكتابة للطلبات والرسائل
CREATE POLICY "Enable insert for all users" ON survey_requests FOR INSERT WITH CHECK (true);
CREATE POLICY "Enable insert for all users" ON contact_messages FOR INSERT WITH CHECK (true);

-- سياسات الإدارة (للمستخدمين المصادق عليهم)
CREATE POLICY "Enable all for authenticated users" ON survey_requests FOR ALL USING (auth.role() = 'authenticated');
CREATE POLICY "Enable all for authenticated users" ON categories FOR ALL USING (auth.role() = 'authenticated');
CREATE POLICY "Enable all for authenticated users" ON surveys FOR ALL USING (auth.role() = 'authenticated');
CREATE POLICY "Enable all for authenticated users" ON contact_messages FOR ALL USING (auth.role() = 'authenticated');
CREATE POLICY "Enable all for authenticated users" ON site_visits FOR ALL USING (auth.role() = 'authenticated');
```

### إنشاء حساب مشرف

في لوحة تحكم Supabase، انتقل إلى **Authentication** > **Users** وقم بإنشاء مستخدم جديد:

- Email: admin@example.com (أو أي بريد تريده)
- Password: كلمة مرور قوية

## 📖 الاستخدام

### تشغيل المشروع

1. افتح ملف `index.html` في المتصفح
2. أو استخدم خادم محلي مثل:
   ```bash
   # باستخدام Python
   python -m http.server 8000
   
   # باستخدام Node.js
   npx http-server
   ```

### الوصول إلى لوحة التحكم

1. افتح `admin.html`
2. سجل الدخول باستخدام بيانات المشرف التي أنشأتها في Supabase
3. ستظهر لك لوحة التحكم الكاملة

## 📄 الصفحات

### 1. الصفحة الرئيسية (`index.html`)

- **Hero Section**: عرض شعار المنصة وزر الدعوة للعمل
- **التصنيفات**: شريط أفقي لتصفية الاستبيانات
- **الاستبيانات**: عرض بطاقات الاستبيانات المتاحة
- **التواصل**: نموذج للتواصل مع الإدارة

### 2. صفحة طلب النشر (`request.html`)

نموذج شامل يحتوي على:
- اسم مقدم الطلب
- البريد الإلكتروني
- عنوان الاستبيان
- وصف الاستبيان
- اختيار التصنيف
- رابط الاستبيان

### 3. لوحة التحكم (`admin.html`)

#### الإحصائيات
- عدد زيارات المنصة
- عدد طلبات الاستبيانات
- عدد الرسائل الواردة
- عدد الاستبيانات المنشورة

#### التبويبات
1. **طلبات الاستبيانات**: عرض وإدارة الطلبات (قبول/رفض)
2. **إدارة التصنيفات**: إضافة وتعديل وحذف التصنيفات
3. **الرسائل**: عرض رسائل التواصل
4. **الاستبيانات المنشورة**: عرض وإدارة الاستبيانات

## 🔒 الأمان

- استخدام Row Level Security (RLS) في Supabase
- تنظيف المدخلات من XSS
- المصادقة عبر Supabase Auth
- سياسات وصول محددة لكل جدول

## 🎨 التصميم

- تصميم عربي كامل (RTL)
- واجهة حديثة وبسيطة
- متجاوب مع جميع الأجهزة (Responsive)
- ألوان متناسقة ومريحة للعين
- تأثيرات انتقالية سلسة

## 📝 ملاحظات مهمة

1. **قبل الاستخدام**: تأكد من تحديث معلومات Supabase في `config.js`
2. **قاعدة البيانات**: قم بتشغيل جميع أوامر SQL المطلوبة
3. **حساب المشرف**: أنشئ حساب مشرف في Supabase Authentication
4. **الأمان**: لا تشارك مفاتيح Supabase الخاصة (Service Role Key)

## 🚀 التطوير المستقبلي

يمكن تطوير المنصة بإضافة:
- نظام تقييم للاستبيانات
- إحصائيات متقدمة للمشرفين
- نظام إشعارات
- تصدير البيانات
- واجهة برمجية (API)
- نظام بحث متقدم

## 📞 الدعم

للمساعدة أو الاستفسارات، يمكنك:
- استخدام نموذج التواصل في المنصة
- مراجعة توثيق Supabase: https://supabase.com/docs

## 📜 الترخيص

هذا المشروع مفتوح المصدر ومتاح للاستخدام الشخصي والتجاري.

---

**تم التطوير بـ ❤️ لخدمة المجتمع البحثي العربي**
#   m a j m u e  
 