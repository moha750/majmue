# دليل نظام الروابط الداخلية - منصة مجموع

## نظرة عامة

تم تطبيق نظام الروابط الداخلية لحماية روابط الاستبيانات الأصلية وتتبع عدد الضغطات على كل استبيان.

## الميزات الرئيسية

### 1. الروابط الداخلية (Internal Links)
- **لا يتم عرض الرابط الأصلي** (Google Forms أو غيره) في الموقع أبداً
- يتم توليد **slug فريد** لكل استبيان (مثل: `abc123xy`)
- الرابط الداخلي: `survey.html?s=abc123xy`
- هذا هو الرابط الوحيد الذي يظهر للمستخدمين

### 2. تتبع الضغطات (Click Tracking)
- يتم زيادة عداد `click_count` تلقائياً عند كل زيارة
- يمكن للمشرف رؤية عدد الضغطات في لوحة التحكم
- الإحصائيات دقيقة ومحدثة في الوقت الفعلي

### 3. التحكم في حالة الاستبيان (Active/Inactive)
- **نشط (Active)**: الاستبيان يظهر في الموقع ويمكن الوصول إليه
- **متوقف (Inactive)**: الاستبيان مخفي ولا يمكن الوصول إليه
- يمكن للمشرف تفعيل/إيقاف أي استبيان من لوحة التحكم

## آلية العمل

### عند قبول طلب نشر استبيان:

1. **توليد Slug فريد**
   - يتم توليد كود عشوائي من 8 أحرف (a-z, 0-9)
   - التحقق من عدم وجود slug مشابه في قاعدة البيانات
   - حفظ الـ slug مع بيانات الاستبيان

2. **حفظ البيانات**
   ```javascript
   {
     title: "عنوان الاستبيان",
     description: "الوصف",
     category_id: 1,
     survey_link: "https://forms.google.com/...", // مخفي
     public_slug: "abc123xy", // ظاهر
     click_count: 0,
     is_active: true
   }
   ```

3. **عرض الرابط الداخلي**
   - في الصفحة الرئيسية: `survey.html?s=abc123xy`
   - في لوحة التحكم: الرابط الكامل مع النطاق

### عند دخول المستخدم على الرابط الداخلي:

1. **التحقق من الاستبيان**
   - البحث عن الاستبيان باستخدام الـ slug
   - التحقق من أن `is_active = true`

2. **زيادة عداد الضغطات**
   - `click_count = click_count + 1`
   - التحديث في قاعدة البيانات

3. **إعادة التوجيه**
   - عرض رسالة "جاري التوجيه..."
   - إعادة توجيه تلقائية بعد ثانيتين إلى الرابط الأصلي

### عند إيقاف الاستبيان:

1. **تحديث الحالة**
   - `is_active = false`

2. **منع الوصول**
   - أي محاولة للدخول تُظهر رسالة "الاستبيان غير متاح حالياً"
   - لا يتم إعادة التوجيه
   - لا يتم زيادة عداد الضغطات

## هيكل قاعدة البيانات

### جدول surveys - الحقول الجديدة:

```sql
public_slug TEXT UNIQUE NOT NULL    -- الرابط الداخلي الفريد
click_count INTEGER DEFAULT 0       -- عدد الضغطات
is_active BOOLEAN DEFAULT true      -- حالة الاستبيان
```

## الملفات المُضافة/المُعدّلة

### ملفات جديدة:
1. **`survey.html`** - صفحة إعادة التوجيه
2. **`js/survey.js`** - منطق إعادة التوجيه وتتبع الضغطات
3. **`UPDATE_DATABASE.sql`** - سكريبت تحديث قاعدة البيانات

### ملفات مُعدّلة:
1. **`SUPABASE_SETUP.sql`** - تحديث هيكل جدول surveys
2. **`js/admin.js`** - إضافة:
   - دالة `generateUniqueSlug()`
   - دالة `toggleSurveyStatus()`
   - تحديث `approveRequest()` لتوليد slug
   - تحديث `renderPublishedSurveys()` لعرض الروابط الداخلية

3. **`js/main.js`** - تحديث:
   - `loadSurveys()` لجلب الاستبيانات النشطة فقط
   - `renderSurveys()` لعرض الروابط الداخلية

4. **`admin.html`** - إضافة أعمدة:
   - عدد الضغطات
   - الحالة (نشط/متوقف)

## خطوات التطبيق

### 1. تحديث قاعدة البيانات

إذا كانت قاعدة البيانات موجودة مسبقاً، قم بتشغيل:
```sql
-- في SQL Editor في Supabase
-- نسخ ولصق محتوى ملف UPDATE_DATABASE.sql
```

إذا كنت تنشئ قاعدة بيانات جديدة:
```sql
-- استخدم ملف SUPABASE_SETUP.sql المُحدّث
```

### 2. اختبار النظام

1. **قبول طلب استبيان**
   - سجل دخول إلى لوحة التحكم
   - اذهب إلى "طلبات الاستبيانات"
   - اقبل أي طلب
   - تحقق من توليد slug تلقائياً

2. **اختبار الرابط الداخلي**
   - اذهب إلى الصفحة الرئيسية
   - انقر على "المشاركة في الاستبيان"
   - يجب أن ترى صفحة التوجيه
   - يجب أن يتم التوجيه تلقائياً

3. **اختبار تتبع الضغطات**
   - ارجع إلى لوحة التحكم
   - اذهب إلى "الاستبيانات المنشورة"
   - تحقق من زيادة عدد الضغطات

4. **اختبار إيقاف الاستبيان**
   - في لوحة التحكم، انقر "إيقاف"
   - حاول الدخول على الرابط الداخلي
   - يجب أن ترى رسالة "غير متاح"

## لوحة التحكم - الميزات الجديدة

### جدول الاستبيانات المنشورة:

| العمود | الوصف |
|--------|-------|
| التاريخ | تاريخ النشر |
| العنوان | عنوان الاستبيان |
| التصنيف | تصنيف الاستبيان |
| الرابط الداخلي | الرابط الكامل للمشاركة |
| عدد الضغطات | عدد المرات التي تم فيها الدخول |
| الحالة | نشط / متوقف |
| الإجراءات | تفعيل/إيقاف، حذف |

### الأزرار:

- **تفعيل**: يظهر عندما يكون الاستبيان متوقفاً
- **إيقاف**: يظهر عندما يكون الاستبيان نشطاً
- **حذف**: حذف الاستبيان نهائياً

## الأمان والخصوصية

### ✅ المزايا الأمنية:

1. **حماية الروابط الأصلية**
   - الرابط الأصلي غير ظاهر في الكود المصدري
   - لا يمكن للمستخدمين رؤية الرابط الأصلي

2. **التحكم الكامل**
   - يمكن إيقاف أي استبيان فوراً
   - لا يمكن الوصول للاستبيانات المتوقفة

3. **تتبع دقيق**
   - معرفة عدد الزيارات الفعلية
   - إحصائيات موثوقة

## استكشاف الأخطاء

### المشكلة: لا يتم توليد slug

**الحل:**
- تحقق من تحديث جدول surveys في قاعدة البيانات
- تأكد من إضافة حقل `public_slug`

### المشكلة: خطأ عند إعادة التوجيه

**الحل:**
- تحقق من أن `is_active = true`
- تأكد من وجود الاستبيان في قاعدة البيانات
- افحص Console للأخطاء

### المشكلة: لا يتم تحديث عداد الضغطات

**الحل:**
- تحقق من صلاحيات Row Level Security
- تأكد من تحديث حقل `click_count` في قاعدة البيانات

## ملاحظات مهمة

1. **الروابط القديمة**: إذا كان لديك استبيانات منشورة قبل هذا التحديث، قم بتشغيل `UPDATE_DATABASE.sql`

2. **النسخ الاحتياطي**: احتفظ بنسخة احتياطية من قاعدة البيانات قبل التحديث

3. **الاختبار**: اختبر النظام بالكامل قبل النشر الفعلي

## الدعم

للمزيد من المعلومات، راجع:
- `README.md` - التوثيق الشامل
- `SETUP_GUIDE.md` - دليل الإعداد
- `SUPABASE_SETUP.sql` - هيكل قاعدة البيانات

---

**تم التطوير بنجاح! 🎉**
